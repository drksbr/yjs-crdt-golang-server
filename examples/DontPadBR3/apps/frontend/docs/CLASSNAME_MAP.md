## Apêndice: mapa completo de className (fonte de verdade)

Este apêndice lista cada ocorrência de `className` em `app/` e `components/` com o trecho exato (incluindo expressões/templating).

Para regenerar: `node scripts/extract-classnames.mjs`

### app/(home)/page.tsx

- L25:

```tsx
className="min-h-screen bg-slate-50 dark:bg-slate-950 flex flex-col font-sans text-slate-900 dark:text-slate-100 transition-colors"
```

- L27:

```tsx
className="border-b border-slate-200 dark:border-slate-800 bg-white/80 dark:bg-slate-900/80 backdrop-blur-lg sticky top-0 z-10"
```

- L28:

```tsx
className="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between"
```

- L29:

```tsx
className="flex items-center gap-3"
```

- L30:

```tsx
className="w-8 h-8 bg-slate-900 dark:bg-slate-100 rounded flex items-center justify-center"
```

- L31:

```tsx
className="text-white dark:text-slate-900 font-bold text-sm tracking-tighter"
```

- L33:

```tsx
className="text-lg font-semibold text-slate-900 dark:text-slate-100 tracking-tight"
```

- L37:

```tsx
className="flex gap-8 text-sm font-medium"
```

- L40:

```tsx
className="text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-100 transition no-underline"
```

- L46:

```tsx
className="text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-100 transition no-underline"
```

- L52:

```tsx
className="text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-100 transition no-underline"
```

- L61:

```tsx
className="flex-1"
```

- L62:

```tsx
className="flex flex-col items-center justify-center px-6 py-28 bg-gradient-to-b from-slate-50 dark:from-slate-950 to-white dark:to-slate-900"
```

- L63:

```tsx
className="text-center mb-12 max-w-3xl animate-fade-in-up"
```

- L64:

```tsx
className="text-4xl md:text-5xl lg:text-6xl font-bold text-slate-900 dark:text-slate-100 mb-6 tracking-tight leading-tight"
```

- L66:

```tsx
className="text-slate-500 dark:text-slate-400 relative inline-block min-h-[1.2em] pl-8"
```

- L70:

```tsx
className="text-lg text-slate-600 dark:text-slate-400 max-w-2xl mx-auto leading-relaxed"
```

- L78:

```tsx
className="w-full max-w-md mb-16 animate-fade-in-up animation-delay-200"
```

- L79:

```tsx
className="flex gap-2 shadow-lg dark:shadow-slate-900/50 p-1.5 bg-white dark:bg-slate-800 rounded-xl border border-slate-200 dark:border-slate-700 hover:shadow-xl transition-shadow duration-300"
```

- L80:

```tsx
className="flex-1 flex items-center px-4"
```

- L81:

```tsx
className="text-slate-400 dark:text-slate-500 mr-1"
```

- L87:

```tsx
className="flex-1 py-3 bg-transparent border-none focus:outline-none text-slate-900 dark:text-slate-100 placeholder-slate-400 dark:placeholder-slate-500"
```

- L93:

```tsx
className="px-8 py-3 bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 rounded-lg font-medium hover:bg-slate-800 dark:hover:bg-slate-200 transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed hover:shadow-md"
```

- L98:

```tsx
className="text-xs text-slate-500 dark:text-slate-400 mt-3 text-center"
```

- L106:

```tsx
className="py-24 px-6 bg-slate-50 dark:bg-slate-950 border-t border-slate-200 dark:border-slate-800"
```

- L107:

```tsx
className="max-w-6xl mx-auto"
```

- L108:

```tsx
className="text-center mb-12"
```

- L109:

```tsx
className="text-2xl md:text-3xl font-bold text-slate-900 dark:text-slate-100 mb-4"
```

- L112:

```tsx
className="text-slate-600 dark:text-slate-400 max-w-xl mx-auto"
```

- L126:

```tsx
className="border-t border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 py-24 px-6"
```

- L127:

```tsx
className="max-w-6xl mx-auto"
```

- L128:

```tsx
className="text-center mb-16"
```

- L129:

```tsx
className="text-2xl md:text-3xl font-bold text-slate-900 dark:text-slate-100 mb-4"
```

- L132:

```tsx
className="text-slate-600 dark:text-slate-400 max-w-xl mx-auto"
```

- L137:

```tsx
className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-12 gap-y-10"
```

- L142:

```tsx
className="w-6 h-6"
```

- L151:

```tsx
className="w-6 h-6"
```

- L160:

```tsx
className="w-6 h-6"
```

- L169:

```tsx
className="w-6 h-6"
```

- L178:

```tsx
className="w-6 h-6"
```

- L187:

```tsx
className="w-6 h-6"
```

- L197:

```tsx
className="py-20 px-6 bg-slate-900 dark:bg-slate-800 text-white"
```

- L198:

```tsx
className="max-w-4xl mx-auto text-center"
```

- L199:

```tsx
className="text-3xl md:text-4xl font-bold mb-6"
```

- L202:

```tsx
className="text-slate-400 mb-8 text-lg"
```

- L205:

```tsx
className="flex flex-col sm:flex-row gap-4 justify-center"
```

- L208:

```tsx
className="inline-flex items-center justify-center px-8 py-4 bg-white text-slate-900 rounded-lg font-semibold hover:bg-slate-100 transition no-underline"
```

- L214:

```tsx
className="inline-flex items-center justify-center px-8 py-4 border border-slate-700 dark:border-slate-600 text-white rounded-lg font-semibold hover:bg-slate-800 dark:hover:bg-slate-700 transition no-underline"
```

- L224:

```tsx
className="border-t border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 py-12 px-6"
```

- L225:

```tsx
className="max-w-6xl mx-auto flex flex-col md:flex-row justify-between items-center gap-6 text-sm text-slate-500 dark:text-slate-400"
```

- L226:

```tsx
className="flex items-center gap-2"
```

- L227:

```tsx
className="w-5 h-5 bg-slate-900 dark:bg-slate-100 rounded flex items-center justify-center"
```

- L228:

```tsx
className="text-white dark:text-slate-900 font-bold text-xs"
```

- L230:

```tsx
className="font-semibold text-slate-700 dark:text-slate-300"
```

- L232:

```tsx
className="flex items-center gap-6"
```

- L233:

```tsx
className="hover:text-slate-700 dark:hover:text-slate-300 transition no-underline"
```

- L234:

```tsx
className="hover:text-slate-700 dark:hover:text-slate-300 transition no-underline"
```

- L235:

```tsx
className="hover:text-slate-700 dark:hover:text-slate-300 transition no-underline"
```

- L260:

```tsx
className="no-underline group"
```

- L262:

```tsx
className="p-6 rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 hover:border-slate-300 dark:hover:border-slate-600 hover:shadow-lg dark:hover:shadow-slate-900/50 transition-all duration-300 h-full transform hover:-translate-y-1"
```

- L264:

```tsx
className="flex items-center gap-3 mb-3"
```

- L265:

```tsx
className="text-2xl grayscale group-hover:grayscale-0 transition-all duration-300"
```

- L266:

```tsx
className="font-semibold text-slate-900 dark:text-slate-100"
```

- L268:

```tsx
className="text-slate-600 dark:text-slate-400 text-sm leading-relaxed"
```

- L284:

```tsx
className="flex gap-4 group"
```

- L285:

```tsx
className="flex-shrink-0 w-12 h-12 rounded-xl bg-slate-100 dark:bg-slate-800 flex items-center justify-center text-slate-600 dark:text-slate-400 group-hover:bg-slate-900 dark:group-hover:bg-slate-100 group-hover:text-white dark:group-hover:text-slate-900 transition-all duration-300"
```

- L289:

```tsx
className="font-semibold text-slate-900 dark:text-slate-100 text-lg mb-1"
```

- L290:

```tsx
className="text-slate-600 dark:text-slate-400 text-sm leading-relaxed"
```

### app/layout.tsx

- L25:

```tsx
className={inter.className}
```

- L26:

```tsx
className="bg-white dark:bg-slate-900 text-gray-900 dark:text-slate-100 transition-colors"
```

### components/AudioNoteRecorder.tsx

- L132:

```tsx
className="fixed inset-0 bg-black/50 flex items-center justify-center z-50 backdrop-blur-sm px-4"
```

- L133:

```tsx
className="bg-white dark:bg-slate-900 rounded-lg shadow-xl w-full max-w-md border border-slate-200 dark:border-slate-700"
```

- L135:

```tsx
className="px-6 py-4 border-b border-slate-200 dark:border-slate-700"
```

- L136:

```tsx
className="text-lg font-semibold text-slate-900 dark:text-slate-100"
```

- L137:

```tsx
className="text-slate-600 dark:text-slate-400 text-sm mt-1"
```

- L141:

```tsx
className="p-6"
```

- L143:

```tsx
className="bg-slate-50 dark:bg-slate-800 rounded-lg p-6 mb-6 border border-slate-200 dark:border-slate-700"
```

- L144:

```tsx
className="text-center"
```

- L145:

```tsx
className="text-slate-500 dark:text-slate-400 text-xs uppercase tracking-wide mb-2"
```

- L146:

```tsx
className="text-4xl font-mono font-bold text-slate-900 dark:text-slate-100 tracking-tight"
```

- L154:

```tsx
className="mb-6 flex items-center justify-center gap-2"
```

- L155:

```tsx
className="w-2 h-2 bg-red-500 rounded-full animate-pulse"
```

- L156:

```tsx
className="text-slate-700 dark:text-slate-300 text-sm"
```

- L161:

```tsx
className="flex justify-center gap-4 mb-6"
```

- L165:

```tsx
className="w-20 h-20 rounded-full bg-slate-900 dark:bg-slate-100 hover:bg-slate-700 dark:hover:bg-slate-300 text-white dark:text-slate-900 shadow-lg flex items-center justify-center transition transform hover:scale-105"
```

- L190:

```tsx
className="w-14 h-14 rounded-full bg-slate-200 dark:bg-slate-700 hover:bg-slate-300 dark:hover:bg-slate-600 disabled:opacity-50 disabled:cursor-not-allowed text-slate-900 dark:text-slate-100 flex items-center justify-center transition"
```

- L209:

```tsx
className="w-14 h-14 rounded-full bg-slate-900 dark:bg-slate-100 hover:bg-slate-700 dark:hover:bg-slate-300 text-white dark:text-slate-900 flex items-center justify-center transition"
```

- L227:

```tsx
className="w-14 h-14 rounded-full border-2 border-slate-900 dark:border-slate-100 hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-900 dark:text-slate-100 flex items-center justify-center transition"
```

- L246:

```tsx
className="flex gap-3"
```

- L251:

```tsx
className="flex-1 px-4 py-2.5 bg-slate-900 dark:bg-slate-100 hover:bg-slate-700 dark:hover:bg-slate-300 disabled:opacity-50 disabled:cursor-not-allowed text-white dark:text-slate-900 font-medium rounded-md transition text-sm"
```

- L258:

```tsx
className="flex-1 px-4 py-2.5 border border-slate-300 dark:border-slate-600 hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-700 dark:text-slate-300 font-medium rounded-md transition text-sm"
```

### components/AudioNotes.tsx

- L201:

```tsx
className="flex flex-col gap-4 h-full"
```

- L203:

```tsx
className="flex items-center justify-between gap-3"
```

- L204:

```tsx
className="font-semibold text-slate-900 dark:text-slate-100 text-base"
```

- L210:

```tsx
className="flex items-center gap-1.5 px-3 py-1.5 text-sm bg-slate-900 dark:bg-slate-100 hover:bg-slate-700 dark:hover:bg-slate-300 disabled:opacity-50 disabled:cursor-not-allowed text-white dark:text-slate-900 font-medium rounded-md transition"
```

- L235:

```tsx
className="p-3 bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-700 dark:text-red-300 text-sm rounded-md flex items-start gap-2"
```

- L236:

```tsx
className="flex-shrink-0 mt-0.5"
```

- L246:

```tsx
className="flex-1 overflow-y-auto space-y-2"
```

- L248:

```tsx
className="flex flex-col items-center justify-center py-16 text-center"
```

- L249:

```tsx
className="text-slate-300 dark:text-slate-600 mb-3"
```

- L254:

```tsx
className="text-slate-500 dark:text-slate-400 text-sm font-medium"
```

- L257:

```tsx
className="text-slate-400 dark:text-slate-500 text-xs mt-1"
```

- L265:

```tsx
className="flex items-center gap-3 p-3 bg-white dark:bg-slate-800 rounded-md border border-slate-200 dark:border-slate-700 hover:border-slate-300 dark:hover:border-slate-600 transition group"
```

- L270:

```tsx
className="flex-shrink-0 w-9 h-9 rounded-full bg-slate-900 dark:bg-slate-100 hover:bg-slate-700 dark:hover:bg-slate-300 disabled:opacity-50 disabled:cursor-not-allowed text-white dark:text-slate-900 flex items-center justify-center transition"
```

- L298:

```tsx
className="flex-1 min-w-0"
```

- L299:

```tsx
className="text-sm font-medium text-slate-900 dark:text-slate-100 truncate"
```

- L302:

```tsx
className="text-xs text-slate-500 dark:text-slate-400"
```

- L310:

```tsx
className="flex-shrink-0 w-8 h-8 rounded text-slate-400 hover:text-slate-700 dark:hover:text-slate-200 hover:bg-slate-100 dark:hover:bg-slate-700 transition opacity-0 group-hover:opacity-100 disabled:cursor-not-allowed flex items-center justify-center"
```

### components/CollaborationDemo.tsx

- L121:

```tsx
className="w-full max-w-2xl mx-auto"
```

- L123:

```tsx
className="collab-demo shadow-xl"
```

- L125:

```tsx
className="flex items-center justify-between px-4 py-3 border-b border-slate-100 dark:border-slate-700 bg-slate-50/50 dark:bg-slate-800/50"
```

- L126:

```tsx
className="flex items-center gap-2"
```

- L127:

```tsx
className="flex gap-1.5"
```

- L128:

```tsx
className="w-3 h-3 rounded-full bg-slate-300 dark:bg-slate-600"
```

- L129:

```tsx
className="w-3 h-3 rounded-full bg-slate-300 dark:bg-slate-600"
```

- L130:

```tsx
className="w-3 h-3 rounded-full bg-slate-300 dark:bg-slate-600"
```

- L132:

```tsx
className="text-xs text-slate-400 dark:text-slate-500 ml-2 font-mono"
```

- L136:

```tsx
className="flex items-center gap-1"
```

- L140:

```tsx
className="flex items-center gap-1.5 px-2 py-1 rounded-full bg-slate-100 dark:bg-slate-700 animate-fade-in"
```

- L144:

```tsx
className="w-2 h-2 rounded-full"
```

- L147:

```tsx
className="text-xs text-slate-600 dark:text-slate-300"
```

- L154:

```tsx
className="p-6 min-h-[280px] font-mono text-sm bg-white dark:bg-slate-800"
```

- L156:

```tsx
className="flex items-start min-h-[1.75rem]"
```

- L157:

```tsx
className="w-6 text-slate-300 dark:text-slate-600 text-right mr-4 select-none text-xs"
```

- L160:

```tsx
className="flex-1 flex items-center"
```

- L161:

```tsx
className={`${line.text.startsWith('#') ? 'font-semibold text-slate-900 dark:text-slate-100' : 'text-slate-700 dark:text-slate-300'}`}>
                                    {line.text}
                                </span>
                                {!line.completed && index === lines.length - 1 && (
                                    <span
                                        className="w-0.5 h-4 ml-0.5 animate-typing-cursor rounded-full"
                                        style={{ backgroundColor: getUserColor(line.user) }}
                                    ></span>
                                )}
                            </div>
                        </div>
                    ))}
```

- L174:

```tsx
className="flex items-center text-slate-300 dark:text-slate-600"
```

- L175:

```tsx
className="w-6 text-right mr-4 text-xs"
```

- L176:

```tsx
className="w-0.5 h-4 bg-blue-500 animate-typing-cursor rounded-full"
```

- L183:

```tsx
className="text-center text-sm text-slate-500 dark:text-slate-400 mt-4"
```

### components/ConfirmDeleteModal.tsx

- L45:

```tsx
className="fixed inset-0 z-40 bg-black/50 transition-opacity"
```

- L51:

```tsx
className="fixed inset-0 z-50 flex items-center justify-center p-4"
```

- L54:

```tsx
className="w-full max-w-sm bg-white dark:bg-slate-800 rounded-lg shadow-lg border border-slate-200 dark:border-slate-700 animate-in fade-in zoom-in-95 duration-200"
```

- L60:

```tsx
className="px-6 py-4 border-b border-slate-200 dark:border-slate-700"
```

- L63:

```tsx
className="text-lg font-semibold text-slate-900 dark:text-slate-100"
```

- L70:

```tsx
className="px-6 py-4"
```

- L71:

```tsx
className="text-sm text-slate-600 dark:text-slate-400"
```

- L74:

```tsx
className="mt-3 p-3 bg-slate-50 dark:bg-slate-700/50 border border-slate-200 dark:border-slate-600 rounded-md"
```

- L75:

```tsx
className="text-sm font-medium text-slate-900 dark:text-slate-100 break-words"
```

- L82:

```tsx
className="px-6 py-4 border-t border-slate-200 dark:border-slate-700 flex gap-3 justify-end"
```

- L86:

```tsx
className="px-4 py-2 text-sm font-medium text-slate-900 dark:text-slate-100 bg-slate-100 dark:bg-slate-700 hover:bg-slate-200 dark:hover:bg-slate-600 rounded-md transition disabled:opacity-50 disabled:cursor-not-allowed"
```

- L93:

```tsx
className="px-4 py-2 text-sm font-medium text-white bg-red-600 hover:bg-red-700 rounded-md transition disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-2"
```

- L97:

```tsx
className="inline-block w-3 h-3 border-2 border-white border-t-transparent rounded-full animate-spin"
```

### components/DocumentSettings.tsx

- L492:

```tsx
className="fixed inset-0 z-50 flex items-center justify-center p-4"
```

- L495:

```tsx
className="absolute inset-0 bg-black/30 backdrop-blur-sm"
```

- L501:

```tsx
className="relative bg-white dark:bg-slate-800 rounded-xl shadow-xl max-w-lg w-full border border-slate-200/50 dark:border-slate-700/50 overflow-hidden"
```

- L503:

```tsx
className="border-b border-slate-200/50 dark:border-slate-700/50 px-6 py-5 flex items-center justify-between"
```

- L504:

```tsx
className="text-base font-semibold text-slate-900 dark:text-slate-100"
```

- L509:

```tsx
className="p-1.5 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-700 transition text-slate-500 dark:text-slate-400"
```

- L520:

```tsx
className="border-b border-slate-200/50 dark:border-slate-700/50 px-6"
```

- L521:

```tsx
className="flex gap-1"
```

- L526:

```tsx
className={`flex items-center gap-2 px-3 py-3 text-sm font-medium border-b-2 transition ${activeTab === tab.id
                                    ? 'border-slate-900 dark:border-slate-100 text-slate-900 dark:text-slate-100'
                                    : 'border-transparent text-slate-500 dark:text-slate-400 hover:text-slate-700 dark:hover:text-slate-300'
                                    }`}
                            >
                                {tab.icon}
                                <span className="hidden sm:inline">{tab.label}</span>
                            </button>
                        ))}
```

- L539:

```tsx
className="overflow-y-auto max-h-[calc(80vh-140px)]"
```

- L540:

```tsx
className="px-6 py-6 space-y-6"
```

- L544:

```tsx
className={`p-3 rounded-lg text-sm font-medium transition ${messageType === "success"
                                    ? "bg-green-50 dark:bg-green-900/20 text-green-700 dark:text-green-400 border border-green-200/50 dark:border-green-800/50"
                                    : "bg-red-50 dark:bg-red-900/20 text-red-700 dark:text-red-400 border border-red-200/50 dark:border-red-800/50"
                                    }`}
                            >
                                {message}
                            </div>
                        )}
```

- L555:

```tsx
className="space-y-4"
```

- L557:

```tsx
className="flex gap-2"
```

- L563:

```tsx
className="flex-1 px-3 py-2.5 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700/50 focus:outline-none focus:ring-2 focus:ring-slate-400 dark:focus:ring-slate-500 focus:border-transparent text-slate-900 dark:text-slate-100 placeholder-slate-400 dark:placeholder-slate-500 text-sm transition"
```

- L568:

```tsx
className="px-4 py-2.5 text-sm font-medium rounded-lg transition disabled:opacity-50 disabled:cursor-not-allowed bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 hover:bg-slate-800 dark:hover:bg-slate-200"
```

- L575:

```tsx
className="text-xs text-slate-500 dark:text-slate-400"
```

- L581:

```tsx
className="flex items-center justify-center py-8"
```

- L582:

```tsx
className="inline-block w-6 h-6 border-2 border-slate-300 dark:border-slate-600 border-t-slate-600 dark:border-t-slate-300 rounded-full animate-spin"
```

- L585:

```tsx
className="text-center py-8"
```

- L586:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L587:

```tsx
className="text-sm text-slate-500 dark:text-slate-500 mt-1"
```

- L590:

```tsx
className="space-y-2 max-h-64 overflow-y-auto"
```

- L596:

```tsx
className="p-3 bg-slate-50 dark:bg-slate-700/30 rounded-lg border border-slate-200/50 dark:border-slate-600/50 hover:border-slate-300 dark:hover:border-slate-500 transition"
```

- L598:

```tsx
className="flex items-start justify-between gap-3"
```

- L599:

```tsx
className="flex-1 min-w-0"
```

- L600:

```tsx
className="flex items-center gap-2"
```

- L601:

```tsx
className="font-medium text-sm text-slate-800 dark:text-slate-200 truncate"
```

- L605:

```tsx
className="shrink-0 text-[10px] font-medium px-1.5 py-0.5 rounded bg-slate-200 dark:bg-slate-600 text-slate-600 dark:text-slate-300"
```

- L610:

```tsx
className="text-xs text-slate-500 dark:text-slate-400 mt-1"
```

- L614:

```tsx
className="flex gap-1.5 shrink-0"
```

- L618:

```tsx
className="px-2.5 py-1 text-xs font-medium rounded-md transition disabled:opacity-50 bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-400 hover:bg-green-200 dark:hover:bg-green-900/50"
```

- L625:

```tsx
className="px-2 py-1 text-xs font-medium rounded-md transition bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-400 hover:bg-red-200 dark:hover:bg-red-900/50"
```

- L642:

```tsx
className="space-y-4"
```

- L643:

```tsx
className="flex items-center justify-between"
```

- L644:

```tsx
className="text-sm font-medium text-slate-900 dark:text-slate-100"
```

- L648:

```tsx
className={`text-xs font-medium px-2.5 py-1 rounded-full transition ${isProtected
                                            ? "bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-400"
                                            : "bg-slate-100 dark:bg-slate-700/50 text-slate-600 dark:text-slate-400"
                                            }`}
                                    >
                                        {isProtected ? "🔒 Ativo" : "🔓 Inativo"}
                                    </span>
                                </div>

                                {isProtected ? (
                                    <div className="space-y-3">
                                        <p className="text-xs text-slate-600 dark:text-slate-400 leading-relaxed">
                                            Esta nota está protegida por PIN. Clique abaixo para remover a proteção.
                                        </p>
                                        <button
                                            onClick={handleRemovePin}
                                            disabled={isLoading}
                                            className="w-full px-3 py-2.5 text-sm font-medium rounded-lg border transition disabled:opacity-50 disabled:cursor-not-allowed bg-slate-50 dark:bg-slate-700/50 border-slate-200 dark:border-slate-600 text-slate-700 dark:text-slate-200 hover:bg-slate-100 dark:hover:bg-slate-700"
                                        >
                                            {isLoading ? "Removendo..." : "Remover PIN"}
                                        </button>
                                    </div>
                                ) : (
                                    <div className="space-y-3">
                                        <div>
                                            <label className="block text-xs font-medium text-slate-700 dark:text-slate-300 mb-2">
                                                PIN (4-8 dígitos)
                                            </label>
                                            <div className="relative">
                                                <input
                                                    type={showPassword ? "text" : "password"}
                                                    value={pin}
                                                    onChange={(e) => setPin(e.target.value)}
                                                    placeholder="Ex: 1234"
                                                    maxLength={8}
                                                    className="w-full px-3 py-2.5 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700/50 focus:outline-none focus:ring-2 focus:ring-slate-400 dark:focus:ring-slate-500 focus:border-transparent text-slate-900 dark:text-slate-100 placeholder-slate-400 dark:placeholder-slate-500 text-sm transition"
                                                />
                                                <button
                                                    type="button"
                                                    onClick={() => setShowPassword(!showPassword)}
                                                    className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-600 dark:hover:text-slate-300 transition"
                                                    aria-label={showPassword ? "Ocultar PIN" : "Mostrar PIN"}
                                                >
                                                    {showPassword ? (
                                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                                                            <path d="M17.94 17.94A10.07 10.07 0 003.12 12.62c2.77-4.87 7.2-6.18 8.02-6.85a10 10 0 017.7 7.71"></path>
                                                            <line x1="1" y1="1" x2="23" y2="23"></line>
                                                        </svg>
                                                    ) : (
                                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                                                            <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
                                                            <circle cx="12" cy="12" r="3"></circle>
                                                        </svg>
                                                    )}
                                                </button>
                                            </div>
                                        </div>

                                        <div>
                                            <label className="block text-xs font-medium text-slate-700 dark:text-slate-300 mb-2">
                                                Confirmar PIN
                                            </label>
                                            <input
                                                type={showPassword ? "text" : "password"}
                                                value={confirmPin}
                                                onChange={(e) => setConfirmPin(e.target.value)}
                                                placeholder="Repita o PIN"
                                                maxLength={8}
                                                className="w-full px-3 py-2.5 rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-700/50 focus:outline-none focus:ring-2 focus:ring-slate-400 dark:focus:ring-slate-500 focus:border-transparent text-slate-900 dark:text-slate-100 placeholder-slate-400 dark:placeholder-slate-500 text-sm transition"
                                            />
                                        </div>

                                        <button
                                            onClick={handleSavePin}
                                            disabled={isLoading || !pin.trim()}
                                            className="w-full px-3 py-2.5 text-sm font-medium rounded-lg transition disabled:opacity-50 disabled:cursor-not-allowed bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 hover:bg-slate-800 dark:hover:bg-slate-200"
                                        >
                                            {isLoading ? "Salvando..." : "✓ Salvar PIN"}
                                        </button>
                                    </div>
                                )}
                            </div>
                        )}
```

- L734:

```tsx
className="space-y-3"
```

- L735:

```tsx
className="text-sm font-medium text-red-600 dark:text-red-400"
```

- L738:

```tsx
className="text-xs text-slate-600 dark:text-slate-400"
```

- L746:

```tsx
className="w-full px-3 py-2.5 text-sm font-medium rounded-lg border transition disabled:opacity-50 disabled:cursor-not-allowed bg-red-50 dark:bg-red-900/20 border-red-200/50 dark:border-red-800/50 text-red-700 dark:text-red-400 hover:bg-red-100 dark:hover:bg-red-900/30"
```

- L751:

```tsx
className="space-y-2 bg-red-50 dark:bg-red-900/20 border border-red-200/50 dark:border-red-800/50 rounded-lg p-3"
```

- L752:

```tsx
className="text-xs font-medium text-red-700 dark:text-red-400"
```

- L755:

```tsx
className="flex gap-2"
```

- L759:

```tsx
className="flex-1 px-3 py-2 text-xs font-medium rounded-lg border transition disabled:opacity-50 disabled:cursor-not-allowed bg-white dark:bg-slate-700 border-slate-300 dark:border-slate-600 text-slate-900 dark:text-slate-100 hover:bg-slate-50 dark:hover:bg-slate-600"
```

- L766:

```tsx
className="flex-1 px-3 py-2 text-xs font-medium rounded-lg transition disabled:opacity-50 disabled:cursor-not-allowed bg-red-600 text-white hover:bg-red-700"
```

### components/DocumentView.tsx

- L57:

```tsx
className="min-h-screen flex flex-col bg-white dark:bg-slate-900 font-sans text-slate-900 dark:text-slate-100 transition-colors"
```

- L59:

```tsx
className="border-b border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 sticky top-0 z-20"
```

- L60:

```tsx
className="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between"
```

- L61:

```tsx
className="flex items-center gap-4"
```

- L64:

```tsx
className="w-8 h-8 bg-slate-900 dark:bg-slate-100 rounded flex items-center justify-center text-white dark:text-slate-900 font-bold text-sm tracking-tighter hover:text-gray-400 dark:hover:text-slate-600 hover:tracking-tighter transition"
```

- L68:

```tsx
className="flex flex-col"
```

- L69:

```tsx
className="flex items-center gap-1 text-sm font-semibold text-slate-900 dark:text-slate-100 leading-tight"
```

- L72:

```tsx
className="hover:text-slate-700 dark:hover:text-slate-300 transition"
```

- L76:

```tsx
className="inline md:hidden"
```

- L84:

```tsx
className="hidden md:inline"
```

- L88:

```tsx
className="text-slate-300 dark:text-slate-600"
```

- L89:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L97:

```tsx
className="flex items-center gap-2"
```

- L100:

```tsx
className={`flex items-center justify-center gap-2 px-2 py-1.5 rounded-md border transition ${showFiles
```

- L107:

```tsx
className="text-slate-600 dark:text-slate-300"
```

- L111:

```tsx
className="hidden md:inline text-sm font-medium"
```

- L117:

```tsx
className={`flex items-center justify-center gap-2 px-2 py-1.5 rounded-md border transition ${showAudioNotes
```

- L129:

```tsx
className="hidden md:inline text-sm font-medium"
```

- L135:

```tsx
className={`flex items-center justify-center gap-2 px-2 py-1.5 rounded-md border transition ${showSubdocs
```

- L143:

```tsx
className="text-slate-600 dark:text-slate-300"
```

- L149:

```tsx
className="hidden md:inline text-sm font-medium"
```

- L155:

```tsx
className="flex items-center gap-2 px-1 py-1.5 md:px-2 rounded-md border border-slate-200 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 hover:border-slate-300 dark:hover:border-slate-600 transition"
```

- L176:

```tsx
className="hidden md:inline text-sm font-medium"
```

- L181:

```tsx
className="flex items-center gap-2 px-1 py-1.5 md:px-2 rounded-md border border-slate-200 dark:border-slate-700 text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800 hover:border-slate-300 dark:hover:border-slate-600 transition"
```

- L189:

```tsx
className="hidden md:inline text-sm font-medium"
```

- L206:

```tsx
className="flex-1 flex overflow-hidden"
```

- L208:

```tsx
className="flex-1 overflow-auto bg-white dark:bg-slate-900 w-full"
```

- L217:

```tsx
className="fixed inset-0 bg-black/40 z-30 md:hidden"
```

- L228:

```tsx
className={`fixed top-16 right-0 bottom-0 md:w-96 w-full border-l border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 overflow-auto shadow-lg z-40 transition-transform duration-200 ease-out ${showFiles ? 'translate-x-0' : 'translate-x-full md:translate-x-full'
```

- L232:

```tsx
className="flex items-start justify-between p-6 border-b border-slate-200 dark:border-slate-800"
```

- L233:

```tsx
className="text-sm font-semibold text-slate-900 dark:text-slate-100 uppercase tracking-wider"
```

- L236:

```tsx
className="p-1 rounded-md hover:bg-slate-100 dark:hover:bg-slate-800 transition"
```

- L239:

```tsx
className="text-slate-600 dark:text-slate-300"
```

- L246:

```tsx
className="flex-1 overflow-auto p-6"
```

- L253:

```tsx
className={`fixed top-16 right-0 bottom-0 md:w-96 w-full border-l border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 overflow-auto shadow-lg z-40 transition-transform duration-200 ease-out ${showAudioNotes ? 'translate-x-0' : 'translate-x-full md:translate-x-full'
                                }`}
                            style={{ display: showAudioNotes ? 'flex' : 'none', flexDirection: 'column' }}
                        >
                            <div className="flex items-start justify-between p-6 border-b border-slate-200 dark:border-slate-800">
                                <h2 className="text-sm font-semibold text-slate-900 dark:text-slate-100 uppercase tracking-wider">Notas de Áudio</h2>
                                <button
                                    onClick={() => setShowAudioNotes(false)}
                                    className="p-1 rounded-md hover:bg-slate-100 dark:hover:bg-slate-800 transition"
                                    aria-label="Fechar notas de áudio"
                                >
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="text-slate-600 dark:text-slate-300">
                                        <line x1="18" y1="6" x2="6" y2="18"></line>
                                        <line x1="6" y1="6" x2="18" y2="18"></line>
                                    </svg>
                                </button>
                            </div>

                            <div className="flex-1 overflow-auto p-6">
                                <AudioNotes documentId={documentId} subdocumentId={subdocumentName} />
                            </div>
                        </aside>

                        {/* Subdocuments Panel */}
                        <aside
                            className={`fixed top-16 right-0 bottom-0 md:w-96 w-full border-l border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 overflow-auto shadow-lg z-40 transition-transform duration-200 ease-out ${showSubdocs ? 'translate-x-0' : 'translate-x-full md:translate-x-full'
                                }`}
                            style={{ display: showSubdocs ? 'flex' : 'none', flexDirection: 'column' }}
                        >
                            <div className="flex items-start justify-between p-6 border-b border-slate-200 dark:border-slate-800">
                                <h2 className="text-sm font-semibold text-slate-900 dark:text-slate-100 uppercase tracking-wider">Subdocuments</h2>
                                <button
                                    onClick={() => setShowSubdocs(false)}
                                    className="p-1 rounded-md hover:bg-slate-100 dark:hover:bg-slate-800 transition"
                                    aria-label="Fechar subdocs"
                                >
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="text-slate-600 dark:text-slate-300">
                                        <line x1="18" y1="6" x2="6" y2="18"></line>
                                        <line x1="6" y1="6" x2="18" y2="18"></line>
                                    </svg>
                                </button>
                            </div>

                            <div className="flex-1 overflow-auto p-6">
                                <SubdocumentManager documentId={documentId} />
                            </div>
                        </aside>
                    </>
                )}
            </div>

            {/* Voice Chat PoC
            {showVoiceChat && (
                <VoiceChat documentId={documentId} onClose={() => setShowVoiceChat(false)} />
            )} */}

            {/* Document Settings */}
            <DocumentSettings
                documentId={documentId}
                subdocumentName={subdocumentName}
                isOpen={showSettings}
                onClose={() => setShowSettings(false)}
            />
        </div >
    );
}
```

### components/FileManager.tsx

- L368:

```tsx
className="h-full flex flex-col"
```

- L369:

```tsx
className="text-sm font-semibold text-slate-900 dark:text-slate-100 mb-4 uppercase tracking-wider"
```

- L372:

```tsx
className="relative cursor-pointer mb-4"
```

- L378:

```tsx
className="hidden"
```

- L381:

```tsx
className={`inline-flex items-center gap-2 px-3 py-2 rounded-md text-sm font-medium transition-colors ${uploadProgress.some((p) => p.status === 'uploading')
```

- L393:

```tsx
className="mb-4 p-3 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg"
```

- L394:

```tsx
className="text-xs font-semibold text-slate-700 dark:text-slate-300 mb-2 flex items-center gap-2"
```

- L398:

```tsx
className="grid grid-cols-2 gap-2 text-xs text-slate-600 dark:text-slate-400"
```

- L400:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L401:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L404:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L405:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L408:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L409:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L412:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L413:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L415:

```tsx
className="col-span-2"
```

- L416:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L417:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L420:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L421:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L424:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L425:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L427:

```tsx
className="col-span-2 pt-2 border-t border-slate-200 dark:border-slate-700"
```

- L428:

```tsx
className="font-medium text-slate-700 dark:text-slate-300"
```

- L434:

```tsx
className="mb-4 p-3 bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-lg text-red-700 dark:text-red-400 text-sm"
```

- L441:

```tsx
className="mb-4 space-y-2"
```

- L445:

```tsx
className="p-3 bg-slate-50 dark:bg-slate-800 rounded-lg border border-slate-200 dark:border-slate-700"
```

- L447:

```tsx
className="flex items-center justify-between mb-2"
```

- L448:

```tsx
className="text-sm font-medium text-slate-900 dark:text-slate-100 truncate"
```

- L452:

```tsx
className={`text-xs font-semibold ${upload.status === 'success'
```

- L466:

```tsx
className="w-full bg-slate-200 dark:bg-slate-700 rounded-full h-2 overflow-hidden"
```

- L468:

```tsx
className={`h-full transition-all duration-300 ${upload.status === 'success'
                                        ? 'bg-green-500'
                                        : upload.status === 'error'
                                            ? 'bg-red-500'
                                            : 'bg-blue-500'
                                        }`}
                                    style={{ width: `${upload.progress}%` }}
                                />
                            </div>
                            {upload.error && (
                                <p className="text-xs text-red-600 dark:text-red-400 mt-2">
                                    {upload.error}
                                </p>
                            )}
                        </div>
                    ))}
                </div>
            )}
```

- L488:

```tsx
className="text-center py-8 text-slate-500 dark:text-slate-400"
```

- L492:

```tsx
className="text-center py-8 text-slate-500 dark:text-slate-400 text-sm"
```

- L496:

```tsx
className="space-y-2 flex-1 overflow-auto"
```

- L500:

```tsx
className="flex items-center justify-between p-3 bg-slate-50 dark:bg-slate-800 rounded-lg border border-slate-200 dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors"
```

- L502:

```tsx
className="flex-1 min-w-0"
```

- L505:

```tsx
className="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 truncate block text-sm"
```

- L511:

```tsx
className="flex items-center gap-4 mt-1 text-xs text-slate-500 dark:text-slate-400"
```

- L512:

```tsx
className="inline-flex items-center px-2 py-1 bg-slate-200 dark:bg-slate-700 rounded font-semibold text-slate-700 dark:text-slate-300"
```

- L521:

```tsx
className="ml-4 p-1 text-slate-400 dark:text-slate-500 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 rounded transition-colors"
```

### components/Hero.tsx

- L6:

```tsx
className="flex justify-center items-center gap-6"
```

- L12:

```tsx
className="max-w-2xl mx-auto flex flex-col gap-4"
```

- L13:

```tsx
className="text-center text-3xl text-balance "
```

- L22:

```tsx
className="text-lg text-center text-gray-500 text-balance"
```

### components/HeroTypingAnimation.tsx

- L148:

```tsx
className="relative inline-flex items-baseline h-[1.2em]"
```

- L150:

```tsx
className="absolute -left-8 top-[0.55em] -translate-y-1/2 flex items-center gap-1.5 transition-all duration-300"
```

- L152:

```tsx
className="w-2.5 h-2.5 rounded-full animate-pulse"
```

- L159:

```tsx
className="transition-colors duration-200"
```

- L166:

```tsx
className="relative inline-flex items-center self-stretch"
```

- L169:

```tsx
className="inline-block w-[3px] h-[0.85em] ml-0.5 rounded-full transition-all duration-150 self-center"
```

- L178:

```tsx
className="absolute -top-6 left-1/2 -translate-x-1/2 px-2 py-0.5 rounded text-xs font-medium text-white whitespace-nowrap transition-all duration-200 shadow-sm backdrop-blur-sm"
```

- L183:

```tsx
className="ml-1 opacity-70"
```

- L188:

```tsx
className="absolute top-full left-1/2 -translate-x-1/2 w-0 h-0 border-l-4 border-r-4 border-t-4 border-l-transparent border-r-transparent"
```

### components/LiveStatsIndicator.tsx

- L66:

```tsx
className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-slate-900/5 dark:bg-slate-100/5 border border-slate-200 dark:border-slate-700 text-slate-600 dark:text-slate-400 text-sm font-medium mt-8"
```

- L67:

```tsx
className="relative flex h-2.5 w-2.5"
```

- L68:

```tsx
className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"
```

- L69:

```tsx
className="relative inline-flex rounded-full h-2.5 w-2.5 bg-emerald-500"
```

- L72:

```tsx
className={`transition-all duration-300 ${isTransitioning ? 'opacity-0 translate-y-2' : 'opacity-100 translate-y-0'}`}
            >
                <span className="font-semibold text-slate-900 dark:text-slate-100 tabular-nums">
                    {displayValue.toLocaleString('pt-BR')}{currentStat.suffix}
                </span>
                {" "}
                <span className="text-slate-500 dark:text-slate-400">
                    {currentStat.text}
                </span>
            </span>
        </div>
    );
}
```

### components/MetricsSection.tsx

- L66:

```tsx
className="counter-value"
```

- L97:

```tsx
className="py-24 px-6 bg-slate-900 dark:bg-slate-950 text-white overflow-hidden"
```

- L99:

```tsx
className="max-w-6xl mx-auto"
```

- L101:

```tsx
className={`text-center mb-16 ${isVisible ? 'animate-fade-in-up' : 'opacity-0'}`}>
```

- L102:

```tsx
className="text-3xl font-bold mb-4"
```

- L105:

```tsx
className="text-slate-400 max-w-xl mx-auto"
```

- L111:

```tsx
className="grid grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12"
```

- L115:

```tsx
className={`text-center ${isVisible ? 'animate-fade-in-up' : 'opacity-0'}`}
```

- L118:

```tsx
className="text-4xl md:text-5xl font-bold text-white mb-2"
```

- L126:

```tsx
className="text-lg font-semibold text-slate-200 mb-1"
```

- L129:

```tsx
className="text-sm text-slate-400"
```

- L137:

```tsx
className={`mt-20 pt-12 border-t border-slate-800 ${isVisible ? 'animate-fade-in' : 'opacity-0'}`} style={{ animationDelay: '800ms' }}>
                    <div className="flex flex-wrap justify-center items-center gap-8 text-slate-500">
                        <div className="flex items-center gap-2">
                            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                            </svg>
                            <span className="text-sm">Dados criptografados</span>
                        </div>
                        <div className="flex items-center gap-2">
                            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                            <span className="text-sm">Servidores globais</span>
                        </div>
                        <div className="flex items-center gap-2">
                            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 10V3L4 14h7v7l9-11h-7z" />
                            </svg>
                            <span className="text-sm">Sincronização instantânea</span>
                        </div>
                        <div className="flex items-center gap-2">
                            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                            <span className="text-sm">Backup automático</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    );
}
```

### components/PasswordProtection.tsx

- L49:

```tsx
className="fixed inset-0 z-50 flex items-center justify-center p-4"
```

- L51:

```tsx
className="relative w-full max-w-sm"
```

- L53:

```tsx
className="backdrop-blur-xl bg-white/90 dark:bg-slate-800/90 border border-slate-200/50 dark:border-slate-700/50 rounded-2xl shadow-2xl overflow-hidden"
```

- L55:

```tsx
className="relative px-6 pt-8 pb-6 text-center"
```

- L57:

```tsx
className="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-gradient-to-br from-amber-400 to-orange-500 shadow-lg shadow-orange-500/25 mb-5"
```

- L68:

```tsx
className="text-white"
```

- L75:

```tsx
className="text-xl font-bold text-slate-900 dark:text-white mb-1"
```

- L78:

```tsx
className="text-sm text-slate-500 dark:text-slate-400"
```

- L84:

```tsx
className="px-6 pb-8 space-y-4"
```

- L87:

```tsx
className="p-3 rounded-xl bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800/50"
```

- L88:

```tsx
className="text-sm font-medium text-red-600 dark:text-red-400 text-center"
```

- L95:

```tsx
className="relative"
```

- L104:

```tsx
className="w-full px-4 py-4 text-center text-2xl font-bold tracking-[0.5em] rounded-xl border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-900/50 focus:outline-none focus:ring-2 focus:ring-amber-400 focus:border-transparent text-slate-900 dark:text-white placeholder-slate-300 dark:placeholder-slate-600 transition disabled:opacity-50 disabled:cursor-not-allowed"
```

- L110:

```tsx
className="absolute right-3 top-1/2 -translate-y-1/2 p-2 text-slate-400 hover:text-slate-600 dark:hover:text-slate-300 transition disabled:opacity-50"
```

- L131:

```tsx
className="w-full py-3.5 px-4 text-sm font-semibold rounded-xl transition disabled:opacity-50 disabled:cursor-not-allowed bg-gradient-to-r from-amber-500 to-orange-500 hover:from-amber-600 hover:to-orange-600 text-white shadow-lg shadow-orange-500/25 hover:shadow-xl hover:shadow-orange-500/30 active:scale-[0.98]"
```

- L134:

```tsx
className="flex items-center justify-center gap-2"
```

- L135:

```tsx
className="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"
```

- L146:

```tsx
className="text-center text-xs text-slate-400 dark:text-slate-500 mt-4 truncate px-4"
```

### components/Presence.tsx

- L35:

```tsx
className="inline-block relative mx-auto pb-[102px]"
```

- L36:

```tsx
className="flex justify-center items-center gap-2"
```

- L38:

```tsx
className="flex rounded-full border border-dashed p-0.5"
```

- L42:

```tsx
className="block appearance-none w-6 h-6 rounded-full"
```

- L46:

```tsx
className="invisible"
```

- L56:

```tsx
className="rounded-full w-6 h-6"
```

- L62:

```tsx
className="absolute max-w-none mt-[4px] -left-6"
```

### components/SecureDocumentProvider.tsx

- L96:

```tsx
className="min-h-screen flex items-center justify-center bg-white dark:bg-slate-900"
```

- L97:

```tsx
className="text-center"
```

- L98:

```tsx
className="inline-block animate-spin rounded-full h-8 w-8 border-b-2 border-slate-900 dark:border-slate-100 mb-4"
```

- L99:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L108:

```tsx
className="min-h-screen flex flex-col bg-slate-50 dark:bg-slate-950 font-sans text-slate-900 dark:text-slate-100 transition-colors"
```

- L110:

```tsx
className="border-b border-slate-200 dark:border-slate-800 bg-white/80 dark:bg-slate-900/80 backdrop-blur-lg sticky top-0 z-10"
```

- L111:

```tsx
className="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between"
```

- L112:

```tsx
className="flex items-center gap-3"
```

- L115:

```tsx
className="w-8 h-8 bg-slate-900 dark:bg-slate-100 rounded flex items-center justify-center"
```

- L117:

```tsx
className="text-white dark:text-slate-900 font-bold text-sm tracking-tighter"
```

- L119:

```tsx
className="text-lg font-semibold text-slate-900 dark:text-slate-100 tracking-tight"
```

- L127:

```tsx
className="flex-1 flex items-center justify-center px-6 py-16"
```

- L128:

```tsx
className="w-full max-w-md"
```

- L130:

```tsx
className="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-xl shadow-lg dark:shadow-slate-900/50 p-8"
```

- L132:

```tsx
className="flex justify-center mb-6"
```

- L133:

```tsx
className="w-14 h-14 bg-slate-100 dark:bg-slate-800 rounded-full flex items-center justify-center"
```

- L144:

```tsx
className="text-slate-600 dark:text-slate-400"
```

- L153:

```tsx
className="text-center mb-6"
```

- L154:

```tsx
className="text-xl font-semibold text-slate-900 dark:text-slate-100 mb-2"
```

- L157:

```tsx
className="text-sm text-slate-500 dark:text-slate-400"
```

- L163:

```tsx
className="space-y-4"
```

- L166:

```tsx
className="p-3 rounded-lg bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800/50"
```

- L167:

```tsx
className="text-sm text-red-600 dark:text-red-400 text-center"
```

- L174:

```tsx
className="relative"
```

- L183:

```tsx
className="w-full px-4 py-3 text-center text-xl font-medium tracking-[0.3em] rounded-lg border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 focus:outline-none focus:ring-2 focus:ring-slate-900 dark:focus:ring-slate-100 focus:border-transparent text-slate-900 dark:text-white placeholder-slate-300 dark:placeholder-slate-600 transition disabled:opacity-50 disabled:cursor-not-allowed"
```

- L189:

```tsx
className="absolute right-3 top-1/2 -translate-y-1/2 p-1.5 text-slate-400 hover:text-slate-600 dark:hover:text-slate-300 transition disabled:opacity-50"
```

- L210:

```tsx
className="w-full py-3 px-4 text-sm font-semibold rounded-lg transition disabled:opacity-50 disabled:cursor-not-allowed bg-slate-900 dark:bg-slate-100 hover:bg-slate-800 dark:hover:bg-slate-200 text-white dark:text-slate-900 active:scale-[0.98]"
```

- L213:

```tsx
className="flex items-center justify-center gap-2"
```

- L214:

```tsx
className="inline-block w-4 h-4 border-2 border-white dark:border-slate-900 border-t-transparent rounded-full animate-spin"
```

- L225:

```tsx
className="mt-6 text-center"
```

- L228:

```tsx
className="text-sm text-slate-500 dark:text-slate-400 hover:text-slate-700 dark:hover:text-slate-300 transition"
```

### components/SubdocumentManager.tsx

- L117:

```tsx
className="h-full flex flex-col"
```

- L118:

```tsx
className="text-sm font-semibold text-slate-900 dark:text-slate-100 mb-4 uppercase tracking-wider"
```

- L121:

```tsx
className="mb-6 flex flex-col gap-2"
```

- L128:

```tsx
className="px-3 py-2 rounded-md border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-800 focus:outline-none focus:border-slate-500 dark:focus:border-slate-400 focus:ring-1 focus:ring-slate-500 dark:focus:ring-slate-400 text-sm placeholder-slate-400 dark:placeholder-slate-500 text-slate-900 dark:text-slate-100 transition"
```

- L133:

```tsx
className="px-3 py-2 bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 rounded-md hover:bg-slate-800 dark:hover:bg-slate-200 transition disabled:opacity-50 disabled:cursor-not-allowed text-sm font-medium"
```

- L140:

```tsx
className="flex-1 overflow-auto"
```

- L143:

```tsx
className="text-center py-8"
```

- L144:

```tsx
className="text-slate-400 dark:text-slate-500 text-sm"
```

- L147:

```tsx
className="text-slate-400 dark:text-slate-500 text-xs mt-2"
```

- L152:

```tsx
className="space-y-2"
```

- L154:

```tsx
className="group"
```

- L156:

```tsx
className="flex items-start justify-between p-3 rounded-lg border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 hover:bg-slate-100 dark:hover:bg-slate-700 hover:border-slate-300 dark:hover:border-slate-600 transition-all cursor-pointer"
```

- L157:

```tsx
className="flex-1 min-w-0"
```

- L158:

```tsx
className="font-medium text-slate-900 dark:text-slate-100 text-sm truncate"
```

- L161:

```tsx
className="text-xs text-slate-500 dark:text-slate-400 mt-1"
```

- L175:

```tsx
className="ml-2 p-1 text-slate-400 dark:text-slate-500 hover:text-red-600 dark:hover:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 rounded transition opacity-0 group-hover:opacity-100"
```

### components/SyncStatus.tsx

- L39:

```tsx
className="flex items-center gap-2 text-xs text-slate-500 dark:text-slate-400"
```

- L40:

```tsx
className={`h-2 w-2 rounded-full transition-colors duration-300 ${colorClass}`} />
            {showLabel && label && (
                <span className="transition-opacity duration-200">{label}</span>
            )}
            {isConnected && count > 0 && (
                <span className="text-slate-400 dark:text-slate-500">
                    {count} {count === 1 ? "online" : "online"}
                </span>
            )}
        </div>
    );
}
```

### components/TextEditor.tsx

- L142:

```tsx
className="h-full w-full md:max-w-7xl md:mx-auto md:px-6 md:py-4 flex flex-col"
```

- L143:

```tsx
className="flex-1 bg-white dark:bg-slate-800 rounded-lg shadow-sm border border-slate-200 dark:border-slate-700 relative"
```

- L144:

```tsx
className="h-full overflow-auto"
```

### components/Toast.tsx

- L25:

```tsx
className="fixed bottom-6 right-6 bg-slate-900 dark:bg-slate-100 text-white dark:text-slate-900 px-4 py-3 rounded-lg shadow-lg flex items-center gap-2 animate-in fade-in slide-in-from-bottom-4 duration-300 z-50"
```

- L41:

```tsx
className="text-sm font-medium"
```

- L44:

```tsx
className="ml-2 text-slate-300 dark:text-slate-600 hover:text-white dark:hover:text-slate-900 transition"
```

### components/Todos.tsx

- L10:

```tsx
className="flex flex-col p-6"
```

- L11:

```tsx
className="font-bold text-lg mb-2"
```

- L14:

```tsx
className="list-decimal list flex flex-col gap-3 mb-12"
```

- L15:

```tsx
className="grid grid-cols-[auto_1fr] grid-auto-flow-row gap-x-4 gap-y-1 items-center"
```

- L17:

```tsx
className="peer"
```

- L22:

```tsx
className="col-start-2 peer-checked:line-through"
```

- L29:

```tsx
className="col-start-2 text-xs text-gray-500"
```

- L34:

```tsx
className="grid grid-cols-[auto_1fr] grid-auto-flow-row gap-x-4 gap-y-1 items-center"
```

- L36:

```tsx
className="peer"
```

- L41:

```tsx
className="col-start-2 peer-checked:line-through"
```

- L51:

```tsx
className="col-start-2 text-xs text-gray-500"
```

- L56:

```tsx
className="grid grid-cols-[auto_1fr] grid-auto-flow-row gap-x-4 gap-y-1 items-center"
```

- L58:

```tsx
className="peer"
```

- L63:

```tsx
className="col-start-2 peer-checked:line-through"
```

- L70:

```tsx
className="col-start-2 text-xs text-gray-500"
```

- L77:

```tsx
className="font-bold text-lg mb-2"
```

- L78:

```tsx
className="list-decimal list-inside flex flex-col gap-3"
```

- L79:

```tsx
className="grid grid-cols-[auto_1fr] grid-auto-flow-row gap-x-4 gap-y-1 items-center"
```

- L81:

```tsx
className="peer"
```

- L86:

```tsx
className="col-start-2 peer-checked:line-through"
```

- L93:

```tsx
className="col-start-2 text-xs text-gray-500"
```

- L97:

```tsx
className="grid grid-cols-[auto_1fr] grid-auto-flow-row gap-x-4 gap-y-1 items-center"
```

- L99:

```tsx
className="peer"
```

- L104:

```tsx
className="col-start-2 peer-checked:line-through"
```

- L111:

```tsx
className="col-start-2 text-xs text-gray-500"
```

- L116:

```tsx
className="grid grid-cols-[auto_1fr] grid-auto-flow-row gap-x-4 gap-y-1 items-center"
```

- L118:

```tsx
className="peer"
```

- L123:

```tsx
className="col-start-2 peer-checked:line-through"
```

- L126:

```tsx
className="col-start-2 text-xs text-gray-500"
```

- L131:

```tsx
className="grid grid-cols-[auto_1fr] grid-auto-flow-row gap-x-4 gap-y-1 items-center"
```

- L133:

```tsx
className="peer"
```

- L138:

```tsx
className="col-start-2 peer-checked:line-through"
```

- L142:

```tsx
className="col-start-2 text-xs text-gray-500"
```
